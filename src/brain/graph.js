// TODO: Auto-graph entity + edge extraction.
//
// Responsibilities (per SPEC.md §3 + §8):
//   - Given a brain row, ask Claude to extract entities and edges.
//   - Upsert entities into graph_entities (canonical name + aliases).
//   - Insert edges into graph_edges with subject_id, predicate, object_id,
//     source_brain (the brain row id), and confidence.
//
// Implementation comes in the v1 build task.

throw new Error('src/brain/graph.js: not implemented yet — see SPEC.md');
